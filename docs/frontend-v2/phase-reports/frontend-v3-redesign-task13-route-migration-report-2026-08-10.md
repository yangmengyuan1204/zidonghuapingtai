# Task 13 — 原生路由收敛

- 状态：完成。
- 原生页面：dashboard、projects、apiCases、apiHarvester、dataScripts、requirementVerification、uiCases、records、systemRegression、users。
- 迁移：四个剩余业务入口不再挂载 LegacyEmbedView；旧实现保留为回滚资产。
- 验证：route migration 与 legacy embed retirement 契约通过。
